# LogiRoute Global Design System

This folder contains global visual tokens for all spec-driven development files.

## Files

- `colors.md` — global brand, neutral, semantic, and status colors
- `typography.md` — type scale and font rules
- `spacing.md` — spacing, radius, and shadow tokens

## Rule

Every screen spec should reference these files instead of defining its own color scheme.
