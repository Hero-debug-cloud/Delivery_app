# API Configuration
PORT=8000

# Database Configuration (PostgreSQL / TimescaleDB / PostGIS)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/logiroute

# Redis Configuration (Ephemerals / Geospatial Cache)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
